# OData Guide

OData services expose SAP business objects to Fiori/UI clients. A CDS view can be published or bound to a service, generating metadata and entity sets.

Example responsibilities: validate `$metadata`, test CRUD endpoints, check authorizations, monitor Gateway errors, and coordinate activation in `/IWFND/MAINT_SERVICE`.
