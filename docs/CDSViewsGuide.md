# CDS Views Guide

CDS views model reusable business semantics in ABAP. The project includes `ZI_CUSTOMER`, `ZI_SALESORDER`, and `ZI_INCIDENT` examples with annotations and associations.

Recommended practices: stable keys, clear labels, authorization checks, associations instead of manual joins when navigation is needed, and explicit service exposure governance.
